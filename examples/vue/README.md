# Vue example

A Vite + Vue application that consumes `@ogcio/design-system-vue`.

## Run

From the repository root:

```bash
pnpm examples:vue
```

Or from this folder after installing workspace dependencies:

```bash
pnpm dev
```
