---
title: Textarea
description: Textarea HTML Component
status: in-review
libraries:
  - platform: 'figma'
    link: 'https://www.figma.com/design/ZEqfpXhocdZs1K5YfsE5ws/Design-system?node-id=5499-6828'
    status: 'stable'
  - platform: 'global'
    link: '?path=/docs/form-textarea--docs'
    status: 'stable'
  - platform: react
    link: '?path=/docs/form-textarea--docs'
    status: 'stable'
component:
  id: 'textarea'
  properties:
    - name: TextAreaProps
      fields:
        - name: 'ref'
          ofType: 'object'
          description: 'A reference to the text area element.'
          required: false
        - name: 'rows'
          ofType: 'number'
          description: 'The number of visible text lines for the text area.'
          required: false
        - name: 'cols'
          ofType: 'number'
          description: 'The visible width of the text area in characters.'
          required: false
        - name: 'autoComplete'
          ofType: 'string'
          description: 'Specifies whether the browser should automatically complete the input.'
          required: false
        - name: 'maxChars'
          ofType: 'number'
          description: 'The maximum number of characters allowed in the text area.'
          required: false
        - name: 'halfFluid'
          ofType: 'boolean'
          description: 'If `true`, the text area will take up half the width of the container.'
          required: false
---

# Textarea

The `Textarea` component allows users to input multi-line text. It's ideal for longer text fields, such as descriptions, messages, or comments, and offers flexibility in terms of size using `rows` and `cols` props. Additionally, it provides the option to set a character limit using the `maxChars` prop for better control over user input length.

## Variants

### Default textarea

<ComponentContainer>
  <FormField
    hint={{
      text: 'Hint: This is a helpful hint.'
    }}
    label={{
      htmlFor: 'textarea-id-1',
      text: 'Textarea Label'
    }}
  >
    <TextArea
      id="textarea-id-1"
      cols={100}
      rows={4}
    />
  </FormField>
</ComponentContainer>
          
<Tabs id="tab1">
  <TabList>
    <TabItem value="tab11" checked>HTML</TabItem>
    <TabItem value="tab13">React</TabItem>
  </TabList>
  <TabPanel value="tab11">
  ```html
  <fieldset>
    <label class="gi-text-md gi-label gi-mb-1" for="textarea-id-1">Label</label>
    <div class="gi-hint-text-md gi-hint-text gi-mb-1">Hint: This is a helpful hint.</div>
    <div >
      <div class="gi-textarea-container">
        <textarea rows="4" cols="100" autocomplete="on" class="gi-textarea" id="textarea-id-1"></textarea>
      </div>
    </div>
  </fieldset>
  ```
  </TabPanel>
  <TabPanel value="tab13">
  ```jsx
  import { FormField, TextArea } from '@govie-ds/textarea';

  <FormField
    hint={{
      text: 'Hint: This is a helpful hint.'
    }}
    label={{
      htmlFor: 'textarea-id-1',
      text: 'Textarea Label'
    }}
  >
    <TextArea
      id="textarea-id-1"
      cols={100}
      rows={4}
    />
  </FormField>
  ```
  </TabPanel>
</Tabs>

### With Character limit

<ComponentContainer>
  <FormField
    hint={{
      text: 'Hint: This is a helpful hint.',
    }}
    label={{
      htmlFor: 'textarea-id-5',
      text: 'Label',
    }}
  >
    <TextArea data-testid="textarea-id-5" id="textarea-id-5" maxChars={30} />
  </FormField>
</ComponentContainer>

<Tabs id="tab2">
  <TabList>
    <TabItem value="tab21" checked>
      HTML
    </TabItem>
    <TabItem value="tab23">React</TabItem>
  </TabList>
  <TabPanel value="tab21">
    ```html
    <fieldset class="">
      <label class="gi-text-md gi-label gi-mb-1" for="textarea-id-5">
        Label
      </label>
      <div class="gi-hint-text-md gi-hint-text gi-mb-1">
        Hint: This is a helpful hint.
      </div>
      <div>
        <div class="gi-textarea-container" data-module="gieds-textarea">
          <textarea
            rows="4"
            cols="100"
            autocomplete="on"
            class="gi-textarea"
            maxlength="30"
            id="textarea-id-5"
            data-testid="textarea-id-5"
          ></textarea>
        </div>
        <div class="gi-textarea-remaining-chars">
          <div class="gi-hint-text-md gi-hint-text">
            You have 30 characters remaining
          </div>
        </div>
      </div>
    </fieldset>
    ```
  </TabPanel>
  <TabPanel value="tab23">
    ```jsx import {(FormField, TextArea)} from '@govie-ds/textarea';
    <FormField
      hint={{
        text: 'Hint: This is a helpful hint.',
      }}
      label={{
        htmlFor: 'textarea-id-5',
        text: 'Label',
      }}
    >
      <TextArea data-testid="textarea-id-5" id="textarea-id-5" maxChars={30} />
    </FormField>
    ```
  </TabPanel>
</Tabs>

## Properties

<ComponentPropertiesTable componentId="textarea" />

## Usage

### i18n Keys

```json
  "translation": {
    "textarea.remainingChars": "You have {{remainingChars}} characters remaining"
  }
```

### Best Practices

### When to use this component

- **Multi-line input**: Use `Textarea` when the input requires multiple lines of text, such as:
- Feedback forms, comments, or reviews
- Messages, descriptions, or instructions
- Long-form text fields like addresses or notes
- **Variable text length**: Use it when you cannot predict how long the user's input will be, but expect it to exceed a single line.
- **Custom size control**: Use `rows` and `cols` to control the visible size of the text box, tailoring the component to your layout needs.
- **Character limit**: Use `maxChars` to limit the number of characters allowed.

### When not to use this component

- **Single-line input**: If you only expect short, single-line inputs (e.g., names, email addresses), use the `TextInput` component instead.
- **Structured data input**: For specialised inputs like dates, numbers, or selections from predefined options, use components like `DateInput`, `NumberInput`, or `Select` instead of `Textarea`.
- **Large text area**: If the input area becomes too large (like for editing full documents or code), consider a more robust solution, such as a dedicated text editor component or even a rich text editor.

## Status

<ComponentStatusBlock componentId="textarea" />
