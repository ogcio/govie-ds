---
title: Popover React
description: Popover React Component
status: stable
component:
  id: 'popover-react'
  link: '/docs/application-popover--docs' 
  status: 'stable'
  properties:
    - name: Popover
      fields:
        - name: 'triggerRef'
          ofType: 'RefObject<HTMLElement>'
          description: 'A React ref object pointing to the element that triggers the popover. This is used by Popper.js to position the popover relative to the trigger.'
          required: true
        - name: 'children'
          ofType: 'ReactNode'
          description: 'The content to be displayed inside the popover. This will be rendered within the popover container.'
          required: true
        - name: 'className'
          ofType: 'string'
          description: 'Custom class names applied to the main popover `div` element for additional styling.'
          required: false
        - name: 'placement'
          ofType: 'string' 
          description: 'The placement of the popover relative to the `triggerRef` element. Uses Popper.js placement options (e.g., `bottom-start`, `top`, `right-end`).'
          default: 'bottom-start'
          required: false
        - name: 'offset'
          ofType: '[number, number]'
          description: 'An array specifying the skidding and distance offset of the popover from the trigger, in pixels. `[skidding, distance]`'
          default: '[0, 4]'
          required: false
        - name: 'open'
          ofType: 'boolean'
          description: 'Controls the visibility of the popover. Set to `true` to show the popover, `false` to hide it.'
          required: true
        - name: 'onOpenChange'
          ofType: '(isOpen: boolean) => void'
          description: 'Callback function invoked when the popover\'s open state needs to change, typically due to an outside click or the Escape key press.'
          required: true
  stories:
    - name: 'default'
      url: '/story/application-popover--default'
---

# Popover

The Popover component is a versatile UI element used to display contextual information or actions when a user interacts with a specific trigger element. It leverages Popper.js for precise positioning and offers built-in functionality for managing its open/close state through external clicks and keyboard interactions.

<Section current="react" >

<ComponentPreview>
  <PopoverSample />
</ComponentPreview>

## Examples

### Default

The default example demonstrates the basic functionality of the Popover, including opening, closing via outside click, and closing via the Escape key.

<StorybookFrame
  heightClassName="h-max"
  componentId="popover-react"
  story="default"
/>

## Properties

The Popover component accepts the following properties:

<ComponentPropertiesTable componentId="popover-react" />

</Section>
