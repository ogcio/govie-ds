---
title: Chip
description: Chip HTML Component
status: in-review
libraries:
  - platform: 'figma'
    link: 'https://www.figma.com/design/ZEqfpXhocdZs1K5YfsE5ws/Design-system?node-id=1668-2029'
    status: 'stable'
  - platform: 'global'
    link: '?path=/docs/components-chip--docs'
    status: 'stable'
  - platform: 'react'
    link: '?path=/docs/components-chip--docs'
    status: 'stable'
properties:
  - name: ChipProps
    fields:
      - name: 'label'
        ofType: 'string'
        description: 'A string that defines the content displayed inside the chip.'
        required: true
      - name: 'onClose'
        ofType: 'function'
        description: 'A function that gets triggered when the chip''s close action is invoked (e.g., clicking a dismiss button).'
        required: false
---

# Chip

A Chip is a compact UI element that displays information, can be removed via a close button, and is ideal for tags, filters, or selection indicators.
It allow users to close the chip using keyboard interactions. For accessibility, users can press the `Enter` or `Space` key to close the chip when it is focused.

## Variants

### Default

<ComponentContainer>
  <Chip label="Label 1" />
</ComponentContainer>

<ComponentContainer>
  <Chip label="Label 2" />
</ComponentContainer>

<ComponentContainer>
  <Chip label="Label 3" />
</ComponentContainer>

<Tabs id="tab0">
  <TabList>
    <TabItem value="tab01" checked>HTML</TabItem>
    <TabItem value="tab02">React</TabItem>
  </TabList>
  <TabPanel value="tab01">        
    ```html
    <div class="gi-chip" aria-label="chip: Label 1" aria-describedby="chip-description" tabindex="0">
        <span id="chip-description">Label 1</span>
        <div role="button" aria-label="remove chip">
            <span role="presentation" class="material-symbols-outlined gi-block" style="font-size:16px">close</span>
        </div>
    </div>
    ```
  </TabPanel>
  <TabPanel value="tab02">        
    ```html
    import { Chip } from '@govie-ds/list';

    const handleClose = () => {}

    <Chip label="Label 1" onClose={handleClose} />
    ```

  </TabPanel>
</Tabs>

## Usage

<ComponentPropertiesTable componentId="chip" />

### Chip Props

- **label** (required): A string that defines the content displayed inside the chip.

- **onClose** (optional): A function that gets triggered when the chip’s close action is invoked (e.g., clicking a dismiss button).

### Best Practices

### When to use this component

Use the Chip component to display concise information, such as tags or selected items, that users can easily identify and remove. Ideal for filtering, categorizing, or indicating selections in forms and lists.

### When not to use this component

Avoid using the Chip component for lengthy content, essential actions, or complex user interactions. It’s best suited for simple tags and removable items.

## Status

<ComponentStatusBlock componentId="chip" />
