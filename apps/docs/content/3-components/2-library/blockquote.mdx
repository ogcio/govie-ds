---
title: Blockquote
description: Blockquote HTML Component
status: in-review
libraries:
  - platform: 'figma'
    status: 'considering'
  - platform: 'global'
    link: '?path=/docs/typography-blockquote--docs'
    status: 'alpha'
  - platform: 'react'
    link: '?path=/docs/typography-blockquote--docs'
    status: 'alpha'
properties:
  - name: BlockquoteProps
    fields:
      - name: 'children'
        ofType: 'node'
        description: 'The text or component that will be displayed as the main content of the blockquote.'
        required: true
      - name: 'cite'
        ofType: 'string'
        description: 'A string representing the source of the quotation. This can be a URL or a plain text description.'
        required: false
---

# Blockquote

## Variants

### Default

<ComponentContainer>
  <Blockquote>
    It can take up to 8 weeks to register a lasting power of attorney if there
    are no mistakes in the application.
  </Blockquote>
</ComponentContainer>

## Usage

<ComponentPropertiesTable componentId="blockquote" />

### Blockquote Props

- **children** (required): The text or component that will be displayed as the main content of the blockquote.
- **cite** (optional): A string representing the source of the quotation. This can be a URL or a plain text description.

### Best Practices

### When to use this component

Use the inset text component to differentiate a block of text from the content that surrounds it, for example:

- quotes
- examples
- additional information about the page

### When not to use this component

Some users do not notice inset text if it’s used on complex pages or near to other visually prominent elements.
For this reason, avoid using inset text as a way of highlighting very important information that users need to see.

If you need to draw attention to very important content, like legal information, use the warning text component instead.

## Status

<ComponentStatusBlock componentId="blockquote" />
