---
title: Text Input
description: Text Input HTML Component
status: in-review
libraries:
  - platform: 'figma'
    link: 'https://www.figma.com/design/ZEqfpXhocdZs1K5YfsE5ws/Design-system?node-id=4008-3093&t=x9tccZ94Zz8w2Tvu-4'
    status: 'alpha'
  - platform: 'global'
    link: '?path=/docs/form-textinput--docs'
    status: 'alpha'
  - platform: react
    link: '?path=/docs/form-textinput--docs'
    status: 'alpha'
---

# Text Input

The `TextInput` component is used to allow users to enter single-line text, with options to adjust the width dynamically using `halfFluid`, `fullFluid`, or `characterWidth`. It can be used with prefixes, suffixes, and error states for flexible form input scenarios.

## Variants

### Default

<ComponentContainer>
  <FormField
    hint={{
      text: 'Hint: This is a helpful hint.',
    }}
    label={{
      htmlFor: 'text-input-id',
      text: 'Input Label',
    }}
  >
    <TextInput data-testid="text-input-id" id="text-input-id" />
  </FormField>
</ComponentContainer>

<Tabs id="tab3">
  <TabList>
    <TabItem value="tab31" checked>HTML</TabItem>
    <TabItem value="tab33">React</TabItem>
  </TabList>
  <TabPanel value="tab31">        
    ```html
    <fieldset>
      <label class="gi-text-md gi-label gi-mb-1" for="text-input-id">Label</label>
      <div class="gi-hint-text-md gi-hint-text gi-mb-1">Hint: This is a helpful hint.</div>
      <div class="gi-text-input-container">
        <input type="text" class="gi-text-input" id="text-input-id"data-testid="text-input-id">
      </div>
    </fieldset>
    ```
  </TabPanel>
  <TabPanel value="tab33">
    ```jsx
    import { FormField, TextInput } from "@govie-ds/react";
    
    <FormField
      hint={{
        text: 'Hint: This is a helpful hint.'
      }}
      label={{
        htmlFor: 'text-input-id',
        text: 'Input Label'
      }}
    >
      <TextInput
        data-testid="text-input-id"
        id="text-input-id"
      />
    </FormField>
    ```
  </TabPanel>
</Tabs>

### With Reset

<ComponentContainer>
  <FormField
    hint={{
      text: 'Hint: This is a helpful hint.',
    }}
    label={{
      htmlFor: 'text-reset-id',
      text: 'Input Label',
    }}
  >
    <TextInput clearEnabled data-testid="text-reset-id" id="text-reset-id" />
  </FormField>
</ComponentContainer>

### With Prefix/Suffix

<ComponentContainer>
  <FormField
    hint={{
      text: 'Hint: This is a helpful hint.',
    }}
    label={{
      htmlFor: 'text-input-id',
      text: 'Input Label',
    }}
  >
    <TextInput
      data-testid="text-input-id"
      id="text-input-id"
      prefix="€"
      suffix="kg"
    />
  </FormField>
</ComponentContainer>

### With Icons

<ComponentContainer>
  <FormField
    hint={{
      text: 'Hint: This is a helpful hint.',
    }}
    label={{
      htmlFor: 'text-input-id',
      text: 'Input Label',
    }}
  >
    <TextInput
      data-testid="text-input-id"
      id="text-input-id"
      iconStart="add_circle"
      iconEnd="add_circle"
    />
  </FormField>
</ComponentContainer>

## Usage

#### Features:

- Includes a built-in reset button to clear the input field.
- Maintains the same styling and customisation options as the standard input component.
- Ideal for search fields, dynamic form inputs, or scenarios requiring frequent text updates.

### clearEnabled

The `clearEnabled` prop enhances user experience by displaying a reset (clear) button inside the input field. When enabled, users can quickly clear the input value with a single click. This is particularly useful in forms where users frequently modify or re-enter information. It ensures fast interaction without requiring manual deletion.

### Best Practices

### When to Use This Component

- **Single-Line Text Inputs**: Use this component for short, single-line inputs such as names, email addresses, phone numbers, and short codes (e.g., postal codes).

- **Customizable Widths**: Utilise the `halfFluid` or `fullFluid` properties for inputs that need to fit in responsive layouts. The `characterWidth` prop allows you to specify a custom width based on the number of characters the input should accommodate.

- **Error States**: For forms where invalid input needs to be flagged, the `hasError` prop visually indicates errors with a red border.

- **Custom Prefixes or Suffixes**: This component supports adding contextual information next to the input (e.g., units, symbols) using `prefix` and `suffix` props.

### When Not to Use This Component

- **Multi-Line Text Fields**: If your input requires multiple lines (such as a message box), use a `textarea` instead of `TextInput`.
- **Input with Formatting Requirements**: If input masking or format validation (like date pickers or rich text editors) is necessary, consider using a specialised component.

- **Non-Textual Inputs**: This component is not suitable for non-textual input types like checkboxes, radio buttons, or dropdown menus.

## Status

<ComponentStatusBlock componentId="text-input" />
