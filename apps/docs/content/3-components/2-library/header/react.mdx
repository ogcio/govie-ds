---
title: Header
description: Header React Component
status: stable
component:
  id: 'header-react'
  link: '/docs/layout-header--docs'
  status: 'stable'
  stories:
    - name: 'default'
      url: '/story/layout-header--default'
    - name: 'govie'
      url: '/story/layout-header--govie'
    - name: 'light'
      url: '/story/layout-header--light'
  properties:
    - name: HeaderProps
      fields:
        - name: 'fullWidth'
          ofType: 'boolean'
          description: 'If true, the header will span the full width of the container.'
          required: false
        - name: 'variant'
          ofType: 'string'
          description: 'Controls the visual style of the component ("default", "light").'
          required: false
        - name: '[HTMLAttributes<header>]'
          ofType: '…'
          description: All native header props.
          required: false

    - name: HeaderLogoProps
      fields:
        - name: 'children'
          ofType: 'ReactNode'
          description: Usually <img />.
          required: false
    - name: HeaderTitleProps
      fields:
        - name: 'children'
          ofType: 'ReactNode'
          description: Title content.
          required: true
        - name: '[HTMLAttributes<div>]'
          ofType: '…'
          description: Native div props.
          required: false
    - name: HeaderSecondaryMenuProps
      description: Secondary nav (top row). Renders a <nav>.
      fields:
        - name: 'children'
          ofType: 'ReactNode'
          description: Only HeaderMenuItemLink or HeaderMenuItemSlot.
          required: false
        - name: '[HTMLAttributes<nav>]'
          ofType: '…'
          description: All native nav props.
          required: false
    - name: HeaderPrimaryMenuProps
      description: Primary nav (main actions). Renders a <nav>.
      fields:
        - name: 'children'
          ofType: 'ReactNode'
          description: HeaderMenuItemLink | HeaderMenuItemButton | HeaderMenuItemSeparator.
          required: false
        - name: '[HTMLAttributes<nav>]'
          ofType: '…'
          description: All native nav props.
          required: false
    - name: HeaderMenuItemLinkProps
      fields:
        - name: 'showItemMode'
          ofType: '"always" | "mobile-only" | "desktop-only"'
          description: Visibility breakpoint behavior.
          required: false
        - name: 'icon'
          ofType: 'IconId'
          description: Optional trailing icon (primary menu).
          required: false
        - name: 'external'
          ofType: 'boolean'
          description: Forces external link behavior (adds target/rel).
          required: false
        - name: '[AnchorHTMLAttributes<a>]'
          ofType: '…'
          description: All native anchor props.
          required: false

    - name: HeaderMenuItemButtonProps
      fields:
        - name: 'showItemMode'
          ofType: '"always" | "mobile-only" | "desktop-only"'
          description: Visibility breakpoint behavior.
          required: false
        - name: 'icon'
          ofType: 'IconId'
          description: Leading icon.
          required: false
        - name: '[ButtonHTMLAttributes<button>]'
          ofType: '…'
          description: All native button props.
          required: false

    - name: HeaderMenuItemSeparatorProps
      fields:
        - name: '[HTMLAttributes<div>]'
          ofType: '…'
          description: All native div props".
          required: false
    - name: HeaderMenuItemSlotProps
      fields:
        - name: 'children'
          ofType: 'ReactNode'
          description: Custom content.
          required: true
        - name: '[HTMLAttributes<div>]'
          ofType: '…'
          description: All native div props.
          required: false
---

# Header

The Header component functions as a top-level navigation element for digital services across government departments and public sector organisations. It offers users a consistent and recognisable interface that builds trust, supports a unified brand identity, and delivers key navigation tools. Originally referred to as the GOV.IE masthead, this component is designed to signal to users that they are engaging with an official government service while providing easy access to essential site-wide features. It is adaptable for use beyond GOV.IE, supporting a broad range of departmental and agency websites.

<Section current="react" >

<ComponentPreview className="block w-full gi-not-prose">
  <HeaderComposableSample />
</ComponentPreview>

The Header now uses explicit primitives instead of the old single `<Header items secondaryLinks />` props API. Compose the layout with:

- `HeaderLogo`, `HeaderTitle`
- `HeaderPrimaryMenu`, `HeaderSecondaryMenu`
- `HeaderMenuItemLink`, `HeaderMenuItemButton`, `HeaderMenuItemSeparator`
- `HeaderSlotContainer` (for dropdown/inline regions), `DrawerWrapper`/`DrawerBody`/`DrawerFooter` (for dialogs/drawers)

**Why this is better (vs. legacy `items/secondaryLinks`):**

- Precise semantics & a11y: direct control over roles, labels, `aria-*`, and focus order.
- Testability: deterministic targets (IDs, roles) for testing.
- Layout flexibility: exact ordering and responsive visibility through `showItemMode`.

**Quick migration map:**

- `items[].itemType === 'link'` ⟶ `<HeaderMenuItemLink {...} />`
- `items[].itemType === 'divider'` ⟶ `<HeaderMenuItemSeparator />`
- `items[].itemType === 'slot'` (e.g. search) ⟶ `<HeaderMenuItemButton ... aria-controls="X" />` + `<HeaderSlotContainer id="X">…</HeaderSlotContainer>`
- `secondaryLinks[]` ⟶ `<HeaderSecondaryMenu>` with `<HeaderMenuItemLink>` children

## Examples

### Default

<StorybookFrame
  className="h-52 p-1"
  componentId="header-react"
  story="default"
/>

### Govie

<StorybookFrame className="h-52 p-1" componentId="header-react" story="govie" />

### Light

<StorybookFrame className="h-52 p-1" componentId="header-react" story="light" />

## Properties

<ComponentPropertiesTable componentId="header-react" />

## i18n Keys

The Header component supports localisation through i18n keys in React, [find i18n setup here](/components/setup-guides/react/#internationalisation-i18n-guidelines). Below is the list of keys and their default text:

```json
{
  "header.headerSlot": "Slot Container {{index}}",
  "header.toggleActionItem": "Toggle item action for {{item}}",
  "header.search": "Search",
  "header.searchTheWebsite": "Search the website",
  "header.searchForm": "Search Form",
  "header.submitSearch": "Submit search",
  "header.closeMobileMenu": "Close Mobile Menu",
  "header.mobileNavigationMenu": "Mobile Navigation Menu",
  "header.siteHeader": "Site Header",
  "header.goToHomePage": "Go to the home page",
  "header.drawer.close": "Close",
  "header.menu": "Menu"
}
```

These keys represent visible text elements. You can customise their translations for each locale to enable multilingual support.

</Section>
