---
title: Link
description: Link HTML Component
status: in-review
libraries:
  - platform: 'figma'
    link: 'https://www.figma.com/design/ZEqfpXhocdZs1K5YfsE5ws/Design-system?node-id=4359-2198'
    status: 'beta'
  - platform: 'global'
    link: '?path=/docs/navigation-link--docs'
    status: 'beta'
  - platform: 'react'
    link: '?path=/docs/navigation-link--docs'
    status: 'beta'
---

# Link

Links are blue and underlined by default. If your link is at the end of a sentence or paragraph, make sure that the linked text does not include the full stop.

## Variants

<ComponentContainer>
  <Link href="#">Link with blue color</Link>
</ComponentContainer>

<Tabs id="tab1">
  <TabList>
    <TabItem value="tab11" checked>HTML</TabItem>
    <TabItem value="tab13">React</TabItem>
  </TabList>
  <TabPanel value="tab11">        
    ```html
    <a href="#" class="gi-link"></a>
    ```
  </TabPanel>
  <TabPanel value="tab13">
    ```html
    import { Link } from '@govie-ds/react';

    <Link href="#">This is a sample link</Link>
    ```

  </TabPanel>
</Tabs>

### Link with inherited color

<ComponentContainer>
  <Link href="#" noColor>
    <span>Link with inherited color</span>
  </Link>
</ComponentContainer>

<Tabs id="tab2">
  <TabList>
    <TabItem value="tab21" checked>HTML</TabItem>
    <TabItem value="tab23">React</TabItem>
  </TabList>
  <TabPanel value="tab21">        
    ```html
    <a href="#" class="gi-link gi-link-inherit">Link with inherited color</a>
    ```
  </TabPanel>
  <TabPanel value="tab23">
    ```html
    import { Link } from '@govie-ds/react';

    <Link href="#" noColor>Link with inherited color</Link>
    ```

  </TabPanel>
</Tabs>

### Link with no underline

<ComponentContainer>
  <Link href="#" noUnderline>
    <span>Link with no underline</span>
  </Link>
</ComponentContainer>

<Tabs id="tab3">
  <TabList>
    <TabItem value="tab31" checked>HTML</TabItem>
    <TabItem value="tab33">React</TabItem>
  </TabList>
  <TabPanel value="tab31">        
    ```html
    <a href="#" class="gi-link gi-link-no-underline">Link with no underline</a>
    ```
  </TabPanel>
  <TabPanel value="tab33">
    ```html
    import { Link } from '@govie-ds/react';

    <Link href="#" noUnderline>Link with no underline</Link>
    ```

  </TabPanel>
</Tabs>

### Link with no visited color

<ComponentContainer>
  <Link href="#">Link with no visited color</Link>
</ComponentContainer>

<Tabs id="tab4">
  <TabList>
    <TabItem value="tab41" checked>HTML</TabItem>
    <TabItem value="tab43">React</TabItem>
  </TabList>
  <TabPanel value="tab41">        
    ```html
    <a href="#" class="gi-link gi-link-no-visited">Link with no visited color</a>
    ```
  </TabPanel>
  <TabPanel value="tab43">
    ```html
    import { Link } from '@govie-ds/react';

    <Link href="#" noVisited>Link with no visited color</Link>
    ```

  </TabPanel>
</Tabs>

### Open destination to another page/tab

<ComponentContainer>
  <Link href="#">Open destination to another page/tab</Link>
</ComponentContainer>

<Tabs id="tab5">
  <TabList>
    <TabItem value="tab51" checked>HTML</TabItem>
    <TabItem value="tab53">React</TabItem>
  </TabList>
  <TabPanel value="tab51">        
    ```html
    <a href="#" 
        class="gi-link" 
        rel="noreferrer noopener" 
        target="_blank">
        Open destination to another page/tab
    </a>
    ```
  </TabPanel>
  <TabPanel value="tab53">
    ```html
    import { Link } from '@govie-ds/react';

    <Link href="#" external>Open destination to another page/tab</Link>
    ```

  </TabPanel>
</Tabs>

### With Icons

<ComponentContainer>
  <Link href="#" iconStart="chevron_left" iconEnd="chevron_right">Back</Link>
</ComponentContainer>

<Tabs id="tab6">
  <TabList>
    <TabItem value="tab61" checked>HTML</TabItem>
    <TabItem value="tab63">React</TabItem>
  </TabList>
  <TabPanel value="tab61">        
    ```html
      <a href="#" class="gi-link">
          <span class="gi-link-icon gi-link-icon-start" data-size="sm"><span data-testid="govie-icon" role="presentation" class="gi-block material-symbols-outlined" style="font-size: 16px;">chevron_left</span></span>
          <span class="gi-pl-5 gi-pr-5">Link text</span>
          <span class="gi-link-icon gi-link-icon-end" data-size="sm"><span data-testid="govie-icon" role="presentation" class="gi-block material-symbols-outlined" style="font-size: 16px;">chevron_right</span></span>
      </a>

    ```

  </TabPanel>
  <TabPanel value="tab63">
    ```html
    import { Link } from '@govie-ds/react';

    <Link href="#" iconStart="chevron_left" iconEnd="chevron_right" >Link text</Link>
    ```

  </TabPanel>
</Tabs>

## Usage

### Link Props

- **children** (required): The text or content inside the link. This prop is required and should be a string.

- **href** (required): The URL to which the link points. This prop is required and should be a valid string URL.

- **noUnderline** (optional): Removes the underline from the link.

  - The control for this prop is a boolean, allowing the user to toggle between underlined and non-underlined links.

- **external** (optional): Specifies whether the link should open in a new tab.

  - The control for this prop is a boolean. When set to `true`, the link will open in a new tab.

- **noVisited** (optional): Prevents the link from distinguishing between visited and unvisited states.

  - The control for this prop is a boolean. This can be useful for frequently-changing content where the state of the link should not visually change after being clicked.

- **noColor** (optional): Inherits the color from its parent element.

  - The control for this prop is a boolean. When set to `true`, the link will inherit the color from its parent instead of using its own color.

- **size** (optional): Specifies the size of the link.

  - Options: `sm`, `md` and `lg`.
  - The control for this prop is a string, allowing the user to set the size of the link.

- **iconStart** (optional): Displays an icon before the link text.

  - The control for this prop is a string. You must pass a valid `IconId`.

- **iconEnd** (optional): Displays an icon after the link text.

  - The control for this prop is a string. You must pass a valid `IconId`.

- **disabled** (optional): Disables the link interaction.

  - The control for this prop is a boolean. When `true`, the link appears inactive and does not trigger navigation or actions.

- **appearance** (optional): Sets the visual style of the link.

  - Options: `default` or `light`.
  - The control for this prop is a string.

### With Other Router

It's possible to use the Link component as a React Router Link or NextLink.

<ComponentContainer>
  <Link asChild>
    <NextLink href="/components">Next Link</NextLink>
  </Link>
</ComponentContainer>

<Tabs id="tab7">
  <TabList>
    <TabItem value="tab7">React</TabItem>
  </TabList>
  <TabPanel value="tab7">
    ```html
    import { Link } from '@govie-ds/react';
    import NextLink from 'next/link';

    <Link asChild><NextLink href="/other-page" /></Link>
    ```

  </TabPanel>
</Tabs>

### Best Practices

### When to use this component

Use the link component to create consistent, accessible, and visually appealing hyperlinks throughout your web application.
This component is ideal when you need to provide both internal and external links with customizable styles, such as underlined text and different colors for visited and non-visited states.
The component handles accessibility features like opening external links in a new tab with appropriate rel attributes (`noreferrer` and `noopener`), making it a great choice for scenarios where security and user experience are important.
It also offers styling flexibility with options like no underline (`noUnderline`) and alternative visited link styling (`noVisited`).

### When not to use this component

Avoid using the link component if you require more complex interactivity or behaviour than a standard hyperlink provides, such as navigation within single-page applications (SPAs) where a specialised React Router link component may be more appropriate.
This component is also not suitable for situations where highly customised styles or advanced visual effects are needed that go beyond the default class options provided.
If your design requires additional elements (like icons) or more intricate hover and focus effects that are not supported by the component’s predefined classes, consider creating a custom link element instead.

## Status

<ComponentStatusBlock componentId="link" />
