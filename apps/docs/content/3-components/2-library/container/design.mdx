---
title: 'Container'
description: 'Container HTML Component'
status: 'stable'
component:
  id: 'container'
  status: 'stable'
---

# Container

Use the Container to center page content and keep readable line lengths across breakpoints. It enforces consistent horizontal alignment and optional vertical insets for sections.

<Section current="design">

<ComponentPreview className="not-prose block w-full">
  <Container>
    <p>Container</p>
  </Container>
</ComponentPreview>

## Anatomy & Classes

The Container is a single root **`<div>`** that always applies **`container mx-auto`**: full width up to the design-system max, horizontal **container** padding (unless you turn it off), and **auto margins** to centre the block.

- **`gutters`** (default: **true**): with **false**, **`px-0`** removes horizontal padding from **`container`** (full-bleed content in the same max-width column).
- **`inset`**: with **true**, adds vertical section padding: **`py-4`**, **`md:py-6`**, and **`lg:py-8`**. Omitted or **false** means no extra vertical padding from the component.
- **`maxWidth`**: on top of the default **`container`** width, you can set **`sm`**, **`md`**, **`lg`**, **`xl`**, **`2xl`**, or **`full`**; each adds the corresponding **`max-w-screen-*`** (or **`max-w-full`** for **`full`**). The **default** variant does not add a max-width class and relies on **`container`**.

Use **`className`** for any extra classes. Pass **`id`** and **`dataTestId`** to set the root **`id`** and **`data-testid`**.

## Responsive Inset (`inset`)

When **`inset`** is **true**, top and bottom padding follow the same breakpoint scale as the implementation (mobile-first **Tailwind** / \*\*\*\* padding utilities):

<table>
  <thead>
    <tr>
      <th>Range</th>
      <th>Class</th>
      <th>Typical padding (top and bottom)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        Below the <code>md</code> breakpoint
      </td>
      <td>
        <code>py-4</code>
      </td>
      <td>
        <strong>16px</strong>
      </td>
    </tr>
    <tr>
      <td>
        From <code>md</code> to below <code>lg</code>
      </td>
      <td>
        <code>md:py-6</code>
      </td>
      <td>
        <strong>24px</strong>
      </td>
    </tr>
    <tr>
      <td>
        <code>lg</code> and up
      </td>
      <td>
        <code>lg:py-8</code>
      </td>
      <td>
        <strong>32px</strong>
      </td>
    </tr>
  </tbody>
</table>

## Usage

Use the Container when you need a centralized, consistent wrapper for page sections, ensuring content remains readable and aligned.

### Do

- Use it to provide a centralized, consistent layout wrapper for page content.
- Keep content readable with a capped max-width via **container**.
- Center sections horizontally using **`mx-auto`**.
- Wrap main content areas or any section that should align to the center for a uniform look and feel.
- Target it in automated tests with the **dataTestId** prop to reliably select wrapped content.

### Don't

- Avoid using the container component if you need a flexible or dynamic layout that requires complex positioning, such as grids or flexbox-based structures.
- This component is not suitable for scenarios where you need to override the maximum width or margins frequently, or when the content needs to be fluid across the full width of the viewport.
- If your content does not require centralised alignment or if the predefined styles conflict with the specific requirements of your design, consider using a custom container or a different layout component that offers greater flexibility.

</Section>
