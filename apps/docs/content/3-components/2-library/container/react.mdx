---
title: 'Container'
description: 'Centres page content with consistent horizontal gutters, optional vertical inset, and configurable max width.'
status: 'stable'
component:
  id: 'container-react'
  link: '/story/layout-container--default'
  status: 'stable'
  properties:
    - name: 'ContainerProps'
      fields:
        - name: 'children'
          ofType: 'node'
          description: 'Content rendered inside the container.'
          required: true
        - name: 'id'
          ofType: 'string'
          description: 'Optional id for the root element.'
          required: false
        - name: 'className'
          ofType: 'string'
          description: 'Additional class names merged onto the root element.'
          required: false
        - name: 'inset'
          ofType: 'boolean'
          description: 'When true, applies responsive vertical padding (`py`). Default is false.'
          required: false
        - name: 'gutters'
          ofType: 'boolean'
          description: 'When true, applies responsive horizontal padding (`px`). Default is true.'
          required: false
        - name: 'maxWidth'
          ofType: 'string'
          description: 'Caps the container max width: `sm`, `md`, `lg`, `xl`, `2xl`,`full`, or `default` (responsive container).'
          required: false
        - name: 'role'
          ofType: '"region" | "navigation" | "complementary" | "search" | "form" | "group"'
          description: 'Landmark role for the container. Only set when a landmark semantic is required.'
          required: false
        - name: 'ariaLabel'
          ofType: 'string'
          description: 'Accessible label for the container. Only applied when role is set. Maps to aria-label.'
          required: false
        - name: 'ariaLabelledBy'
          ofType: 'string'
          description: 'Points to the id of an element that labels the container. Preferred over ariaLabel when a visible heading exists. Only applied when role is set. Maps to aria-labelledby.'
          required: false
        - name: 'styles'
          ofType: 'Record<string, string>'
          description: 'Inline styles applied directly to the container element. Use for truly dynamic values that cannot be expressed as Tailwind classes.'
          required: false
        - name: 'dataTestId'
          ofType: 'string'
          description: 'Sets `data-testid` on the root element for tests.'
          required: false
  stories:
    - name: 'default'
      url: '/story/layout-container--default'
    - name: 'with-inset'
      url: '/story/layout-container--with-inset'
    - name: 'gutters-on-and-off'
      url: '/story/layout-container--gutters-on-and-off'
    - name: 'all-max-widths'
      url: '/story/layout-container--all-max-widths'
---

# Container

Use the Container as a layout wrapper when you need centred content, readable line length, and consistent horizontal spacing. It supports optional **gutters** (horizontal padding), an **inset** mode for section-level vertical padding, and a **max width** cap across breakpoints.

<Section current="react">

<ComponentPreview className="gi-not-prose block w-full">
  <Container>
    <p>Container</p>
  </Container>
</ComponentPreview>

### Default

<StorybookFrame componentId="container-react" story="default" />

### With inset

Vertical inset padding is enabled with the `inset` prop.

<StorybookFrame componentId="container-react" story="with-inset" />

### Gutters on and off

Compare default horizontal gutters (`gutters` true) with edge-to-edge content (`gutters` false).

<StorybookFrame componentId="container-react" story="gutters-on-and-off" />

### Max widths

Each `maxWidth` value maps to a corresponding max-width utility (`sm` through `2xl`, or `full`).

<StorybookFrame componentId="container-react" story="all-max-widths" />

## Properties

<ComponentPropertiesTable componentId="container-react" />

</Section>
