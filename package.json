{
  "name": "root",
  "private": true,
  "packageManager": "pnpm@9.1.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "design-system": "nx run @govie-ds/design-system:dev",
    "design-system:build": "nx run @govie-ds/design-system:build",
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all --verbose",
    "tokens:build": "nx run @govie-ds/tokens:build",
    "govie:build": "nx run @govie-ds/theme-govie:build",
    "figma:build": "nx run @govie-ds/figma:build",
    "components:storybook": "nx run @govie-ds/design-components:storybook"
  },
  "devDependencies": {
    "nx": "^18.1.3"
  }
}
