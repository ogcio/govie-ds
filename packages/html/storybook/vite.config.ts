import { defineConfig } from "vite";
import html from "vite-plugin-html";
import nunjucks from "nunjucks";

export default defineConfig({
  //   plugins: [
  //     html({
  //       inject: {
  //         injectData: (data) => nunjucks.renderString(data),
  //       },
  //     }),
  //   ],
});
