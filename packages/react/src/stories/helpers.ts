export const importDisclaimer = (componentName: string) =>
  `\n\nIt is available via the \`next\` entry point of the React package:\n\n\`\`\`tsx\nimport { ${componentName} } from "@ogcio/design-system-react/next";\n\`\`\``;
