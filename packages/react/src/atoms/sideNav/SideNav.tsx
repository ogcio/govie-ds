/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/sideNav/SideNav.lite.tsx`
 */

import * as React from 'react';

export type Props = {
  children?: any;
  ariaLabel?: string;
  className?: string;
  id?: string;
  dataTestId?: string;
};

function SideNav(props: Props) {
  return (
    <nav
      id={props.id}
      className={`gi-max-w-[400px] ${props.className}`}
      aria-label={props.ariaLabel}
      data-testid={props.dataTestId}
    >
      <ul className="gi-list-none gi-p-0 gi-m-0">{props.children}</ul>
    </nav>
  );
}

export default SideNav;
