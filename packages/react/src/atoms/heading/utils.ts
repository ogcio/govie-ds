/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/heading/utils.ts`
 */

import { type Props, Size } from './types';
export const getSize = (size: Props['size'], defaultSize: Props['size']): Props['size'] =>
  size && Object.values(Size).includes(size) ? size : defaultSize;
