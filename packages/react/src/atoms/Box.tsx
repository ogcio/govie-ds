/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/Box.lite.tsx`
 */

import * as React from 'react';

export type Props = LayoutBaseProps;

import type { LayoutBaseProps } from './constants';

function Box(props: Props) {
  return (
    <div
      id={props.id}
      className={props.className}
      role={props.role}
      aria-label={props.role ? props.ariaLabel : undefined}
      aria-labelledby={props.role ? props.ariaLabelledBy : undefined}
      style={props.styles}
      data-testid={props.dataTestId}
    >
      {props.children}
    </div>
  );
}

export default Box;
