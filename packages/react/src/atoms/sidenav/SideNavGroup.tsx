/*
  Mitosis automatically generates this file.
  Do not edit this file directly.
  The file source is in `packages/core/atoms/sidenav/SideNavGroup.lite.tsx`
 */

'use client';
import * as React from 'react';

export type Props = {
  id?: string;
  children: any;
  open?: boolean;
  actions?: any;
  value?: string;
  label: any;
  disabled?: boolean;
  className?: string;
  onClick?: (event: any) => void;
  ariaCurrent?: 'page' | 'step' | 'location' | 'date' | 'time' | 'true' | 'false' | boolean;
  ariaLabel?: string;
  dataTestId?: string;
};

import classes, { actionClasses, arrowClasses } from './SideNavItem.styles';
import GiSideNavRow from './SideNavRow';
import GiKeyboardArrowDownIcon from '../icons/KeyboardArrowDown';
import GiBox from '../Box';

function SideNavGroup(props: Props) {
  props = { open: false, disabled: false, ...props };
  return (
    <>
      <GiSideNavRow>
        <button
          id={props.id}
          className={classes({
            disabled: props.disabled,
            hasAction: !!props.actions,
            className: props.className,
          })}
          value={props.value}
          onClick={(event) => props.onClick && props.onClick(event)}
          data-testid={props.dataTestId}
          aria-current={props.ariaCurrent}
          aria-label={props.ariaLabel}
        >
          <>{props.label || '{props.label}'}</>
        </button>
        {!!props.actions ? (
          <GiBox
            className={actionClasses({
              expandable: true,
            })}
          >
            <>{props.actions || '{props.actions}'}</>
          </GiBox>
        ) : null}
        <GiKeyboardArrowDownIcon
          className={arrowClasses({
            open: props.open,
          })}
        />
      </GiSideNavRow>
      {props.open ? <ul className="gi-side-nav-group">{props.children}</ul> : null}
    </>
  );
}

export default SideNavGroup;
