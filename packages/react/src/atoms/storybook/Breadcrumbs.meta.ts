/*
  Mitosis automatically generates this file.
  Do not edit this file directly.
  The file source is in `packages/core/atoms/storybook/Breadcrumbs.meta.ts`
 */

import type { ArgTypes, StoryContext, Renderer } from 'storybook/internal/types';
import { within, expect } from 'storybook/test';
import type { Props } from '../breadcrumbs/Breadcrumbs';
import { boxMeta } from './Box.meta';
import { checker } from './utilities';
export const breadcrumbsMeta = {
  title: 'Navigation/Breadcrumbs',
  args: {
    ariaLabel: 'Breadcrumbs',
    id: 'breadcrumbs-id',
    dataTestId: 'breadcrumbs',
  },
  argTypes: {
    className: boxMeta.argTypes.className,
    styles: boxMeta.argTypes.styles,
    id: boxMeta.argTypes.id,
    dataTestId: boxMeta.argTypes.dataTestId,
    ariaLabel: {
      control: 'text',
      description: 'Accessible label for the nav landmark. Maps to `aria-label`.',
      table: {
        type: {
          summary: 'string',
        },
      },
    },
    ariaLabelledBy: {
      control: {
        disable: true,
      },
      description:
        'Points to the id of an element that labels the nav. Preferred over `ariaLabel` when a visible heading exists. Maps to `aria-labelledby`.',
      table: {
        type: {
          summary: 'string',
        },
      },
    },
  } satisfies ArgTypes<Props>,
  parameters: {
    docs: {
      description: {
        component:
          'Breadcrumbs help users understand where they are in a site hierarchy and move between levels.\n\n`Breadcrumbs` renders the semantic `<nav>` landmark and an ordered list. Nest `BreadcrumbLink` for each level and `BreadcrumbEllipsis` to collapse intermediate path segments. Mark the current page with `current` on `BreadcrumbLink`.',
      },
    },
  },
};
export const Default = {
  args: breadcrumbsMeta.args,
  play: async ({ canvasElement, step, args }: StoryContext<Renderer>) => {
    const canvas = within(canvasElement as HTMLElement);
    const check = checker(args.dataTestId, canvas, step);
    await check.is('nav');
    await check.attributes({
      id: args.id,
      'aria-label': args.ariaLabel,
    });
    await check.children();
    await step('renders an ordered list of breadcrumb items', async () => {
      const nav = canvas.getByTestId(args.dataTestId);
      expect(nav.querySelector('ol')).toBeInTheDocument();
      const items = canvas.getAllByRole('listitem', {
        hidden: true,
      });
      expect(items).toHaveLength(4);
      expect(items[0]).toHaveTextContent('Home');
      expect(items[2]).toHaveTextContent('Travel');
      expect(items[3]).toHaveTextContent('Documentation');
    });
    await step('renders ellipsis as a visually present, accessibility-hidden item', async () => {
      const ellipsis = canvas.getByTestId('more-horizontal');
      expect(ellipsis).toBeInTheDocument();
      expect(ellipsis.closest('li')).toHaveAttribute('aria-hidden', 'true');
    });
    await step('marks the last item as the current page', async () => {
      expect(
        canvas.getByRole('link', {
          name: 'Documentation',
        }),
      ).toHaveAttribute('aria-current', 'page');
    });
  },
};
