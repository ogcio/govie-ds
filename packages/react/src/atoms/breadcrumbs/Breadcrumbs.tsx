/*
  Mitosis automatically generates this file.
  Do not edit this file directly.
  The file source is in `packages/core/atoms/breadcrumbs/Breadcrumbs.lite.tsx`
 */

import * as React from 'react';

export type Props = {
  id?: string;
  children?: any;
  className?: string;
  styles?: Record<string, string>;
  ariaLabel?: string;
  ariaLabelledBy?: string;
  dataTestId?: string;
};

import { parentList } from './BreadcrumbLink.styles';

function Breadcrumbs(props: Props) {
  return (
    <nav
      id={props.id}
      className={props.className}
      style={props.styles}
      aria-label={props.ariaLabel}
      aria-labelledby={props.ariaLabelledBy}
      data-testid={props.dataTestId}
    >
      <ol className={parentList()}>{props.children}</ol>
    </nav>
  );
}

export default Breadcrumbs;
