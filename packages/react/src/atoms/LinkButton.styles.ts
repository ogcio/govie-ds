/*
  Mitosis automatically generates this file.
  Do not edit this file directly.
  The file source is in `packages/core/atoms/LinkButton.styles.ts`
 */

import { tv } from 'tailwind-variants';
import { baseClasses, ButtonSize, sizeVariants } from './Button.styles';
export default tv({
  extend: baseClasses,
  base: ['gi-gap-2', 'gi-no-underline', 'hover:gi-no-underline'],
  variants: {
    size: sizeVariants,
  },
  defaultVariants: {
    size: ButtonSize.MD,
  },
});
