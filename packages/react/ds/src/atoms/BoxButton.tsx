/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in 'packages/core/atoms/BoxButton'
 */

import * as React from 'react';

export type BoxButtonProps = {
  className?: string;
  variant?: 'primary' | 'secondary';
  onClick?: (event: any) => void;
  children?: any;
};

import DsButton from './DsButton';

function BoxButton(props: BoxButtonProps) {
  return (
    <div
      className={
        'p-4 rounded-md border border-slate-200 ' + (props.className ?? '')
      }
    >
      <DsButton variant={props.variant} onClick={(e) => props.onClick?.(e)}>
        {props.children}
      </DsButton>
    </div>
  );
}

export default BoxButton;
