/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/DsInsetText.lite.tsx`
 */

import * as React from 'react';

export type Props = {
  id?: string;
  children?: any;
  cite?: string;
  ariaDescribedBy?: string;
  ariaLabelledBy?: string;
  ariaLabel?: string;
};

function DsInsetText(props: Props) {
  return (
    <blockquote
      className="gi-p-4 gi-border-l-2xl gi-border-gray-500 gi-text-sm md:gi-text-md gi-not-prose"
      id={props.id}
      cite={props.cite}
      aria-describedby={props.ariaDescribedBy || undefined}
      aria-labelledby={props.ariaLabelledBy || undefined}
      aria-label={
        props.ariaLabelledBy ? undefined : props.ariaLabel || undefined
      }
    >
      {props.children}
    </blockquote>
  );
}

export default DsInsetText;
