/*
  Mitosis automatically generates this file.
  Do not edit this file directly.
  The file source is in `packages/core/atoms/breadcrumbs/BreadcrumbEllipsis.lite.tsx`
 */

import { Component, Input } from '@angular/core';

import { CommonModule } from '@angular/common';

export type Props = Omit<IconProps, 'label'>;

import GiMoreHorizontalIcon from '../icons/MoreHorizontal';
import listClasses, { moreHorizontalClasses } from './BreadcrumbLink.styles';
import type { IconProps } from '../icons/types';

@Component({
  selector: 'gi-breadcrumb-ellipsis',
  template: `
    <li [class]="listClasses()" [attr.aria-hidden]="true">
      <gi-more-horizontal-icon
        [id]="id"
        [className]="
          moreHorizontalClasses({
            className: className,
          })
        "
        [size]="size"
        [color]="color"
        [dataTestId]="dataTestId"
      ></gi-more-horizontal-icon>
    </li>
  `,
  styles: [
    `
      :host {
        display: contents;
      }
    `,
  ],
  standalone: true,
  imports: [CommonModule, GiMoreHorizontalIcon],
})
export default class BreadcrumbEllipsis {
  listClasses = listClasses;
  moreHorizontalClasses = moreHorizontalClasses;

  @Input() id!: Props['id'];
  @Input() className!: Props['className'];
  @Input() size!: Props['size'];
  @Input() color!: Props['color'];
  @Input() dataTestId!: Props['dataTestId'];
}
