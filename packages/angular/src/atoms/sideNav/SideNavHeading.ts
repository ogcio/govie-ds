/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/sideNav/SideNavHeading.lite.tsx`
 */

import { Component, Input } from '@angular/core';

import { CommonModule } from '@angular/common';

export type Props = {
  children?: any;
  secondary?: boolean;
  className?: string;
  id?: string;
  dataTestId?: string;
};

import { tv } from 'tailwind-variants';
import H5 from '../heading/H5';
const headingStyles = tv({
  base: [
    'gi-border-l-sm',
    'gi-text-md',
    'gi-font-bold',
    'gi-mt-2',
    'gi-text-color-text-system-neutral-muted',
    'gi-py-2',
    'gi-border-transparent',
  ],
  variants: {
    secondary: {
      true: 'gi-px-6',
      false: 'gi-px-3',
    },
  },
  defaultVariants: {
    secondary: false,
  },
});

@Component({
  selector: 'gi-side-nav-heading',
  template: `
    <li class="gi-list-none">
      <h-5
        [id]="id"
        [dataTestId]="dataTestId"
        [className]="
          headingStyles({
            secondary: !!secondary,
            class: className,
          })
        "
        ><ng-content></ng-content
      ></h-5>
    </li>
  `,
  styles: [
    `
      :host {
        display: contents;
      }
    `,
  ],
  standalone: true,
  imports: [CommonModule, H5],
})
export default class SideNavHeading {
  headingStyles = headingStyles;
  H5 = H5;

  @Input() id!: Props['id'];
  @Input() dataTestId!: Props['dataTestId'];
  @Input() secondary!: Props['secondary'];
  @Input() className!: Props['className'];
}
