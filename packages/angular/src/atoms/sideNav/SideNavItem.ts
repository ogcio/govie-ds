/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/sideNav/SideNavItem.lite.tsx`
 */

import { Component, Input } from '@angular/core';

import { CommonModule } from '@angular/common';

export type Props = {
  children?: any;
  selected?: boolean;
  expanded?: boolean;
  expandedContentId?: string;
  expandedContent?: any;
  expandedLabel?: string;
  actions?: any;
  id?: string;
  className?: string;
  dataTestId?: string;
};

import { tv } from 'tailwind-variants';
import GiBox from '../Box';
import { styles as buttonBaseStyles } from '../Button';
export const slotStyles = tv({
  extend: buttonBaseStyles,
  base: [
    '!gi-px-3',
    'gi-w-full',
    'focus:gi-outline',
    'focus:gi-outline-sm',
    'focus:gi-outline-color-shadow-intent-focus-default',
    'focus:gi-outline-offset-0',
    'focus:gi-border-solid',
    'focus:gi-border-color-border-intent-focus-default',
    'focus:gi-border-sm',
    'focus:gi-rounded-sm',
    'focus:gi-bg-color-surface-tone-light-fill-hover',
  ],
  variants: {
    hasActions: {
      true: '!gi-pr-10',
      false: '',
    },
    selected: {
      true: [
        'gi-shadow-[inset_4px_0_0_var(--gieds-color-border-tone-primary-accent-selected)]',
        'gi-bg-color-surface-system-neutral-interactive-selected-subtle',
      ],
      false: '',
    },
    variant: {
      flat: '',
    },
    appearance: {
      dark: '',
    },
  },
  defaultVariants: {
    hasActions: false,
    selected: false,
    variant: 'flat',
    appearance: 'dark',
  },
});
export const contentStyles = tv({
  base: ['gi-relative', 'gi-flex', 'gi-items-center', 'gi-justify-between', 'gi-w-full', 'gi-gap-3'],
  variants: {
    primary: {
      true: 'gi-pr-3 gi-font-bold',
      false: 'gi-px-3',
    },
  },
  defaultVariants: {
    primary: true,
  },
});

@Component({
  selector: 'gi-side-nav-item',
  template: `
    <li
      [attr.id]="id"
      [attr.data-testid]="dataTestId"
      [attr.aria-current]="selected ? 'page' : undefined"
      [class]="\`gi-list-none gi-mt-1 \${className ?? ''}\`"
    >
      <gi-box className="gi-relative gi-flex gi-items-center"
        ><ng-content></ng-content>
        <ng-container *ngIf="actions"
          ><gi-box className="gi-absolute gi-right-2">{{ actions }}</gi-box></ng-container
        ></gi-box
      >
      <ng-container *ngIf="expanded && expandedContent"
        ><ul class="gi-list-none gi-p-0 gi-m-0" [attr.id]="expandedContentId" [attr.aria-label]="expandedLabel">
          {{
            expandedContent
          }}
        </ul></ng-container
      >
    </li>
  `,
  styles: [
    `
      :host {
        display: contents;
      }
    `,
  ],
  standalone: true,
  imports: [CommonModule, GiBox],
})
export default class SideNavItem {
  @Input() id!: Props['id'];
  @Input() dataTestId!: Props['dataTestId'];
  @Input() selected!: Props['selected'];
  @Input() className!: Props['className'];
  @Input() actions!: Props['actions'];
  @Input() expanded!: Props['expanded'];
  @Input() expandedContent!: Props['expandedContent'];
  @Input() expandedContentId!: Props['expandedContentId'];
  @Input() expandedLabel!: Props['expandedLabel'];
}
