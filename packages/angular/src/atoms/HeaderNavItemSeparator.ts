/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/HeaderNavItemSeparator.lite.tsx`
 */

import { Component, Input } from '@angular/core';

import { CommonModule } from '@angular/common';

export type Props = {
  className?: string;
  id?: string;
  dataTestId?: string;
};

import { tv } from 'tailwind-variants';
import { dividerStyles } from './Divider';
const separatorStyles = tv({
  extend: dividerStyles,
  base: 'gi-h-8 gi-mx-2 gi-self-center',
  defaultVariants: {
    orientation: 'vertical',
  },
});

@Component({
  selector: 'gi-header-nav-item-separator',
  template: `
    <li
      role="none"
      aria-hidden="true"
      [attr.id]="id"
      [class]="
        separatorStyles({
          className: className,
        })
      "
      [attr.data-testid]="dataTestId"
    ></li>
  `,
  styles: [
    `
      :host {
        display: contents;
      }
    `,
  ],
  standalone: true,
  imports: [CommonModule],
})
export default class HeaderNavItemSeparator {
  separatorStyles = separatorStyles;

  @Input() id!: Props['id'];
  @Input() className!: Props['className'];
  @Input() dataTestId!: Props['dataTestId'];
}
