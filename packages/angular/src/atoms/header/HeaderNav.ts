/**
 * Mitosis automatically generates this file.
 * Do not edit this file directly.
 * The file source is in `packages/core/atoms/header/HeaderNav.lite.tsx`
 */

import { Component, Input } from '@angular/core';

import { CommonModule } from '@angular/common';

export type Props = {
  children: any;
  className?: string;
  styles?: Record<string, string>;
  id?: string;
  ariaLabel: string;
  dataTestId?: string;
};

import { tv } from 'tailwind-variants';
const classes = tv({
  base: 'gi-flex gi-items-center gi-flex-none',
});

@Component({
  selector: 'gi-header-nav',
  template: `
    <nav
      [attr.id]="id"
      [class]="
        classes({
          className: className,
        })
      "
      [ngStyle]="styles"
      [attr.aria-label]="ariaLabel"
      [attr.data-testid]="dataTestId"
    >
      <ul class="gi-list-none gi-flex gi-items-center gi-m-0 gi-p-0 gi-gap-2">
        <ng-content></ng-content>
      </ul>
    </nav>
  `,
  styles: [
    `
      :host {
        display: contents;
      }
    `,
  ],
  standalone: true,
  imports: [CommonModule],
})
export default class HeaderNav {
  classes = classes;

  @Input() id!: Props['id'];
  @Input() className!: Props['className'];
  @Input() styles!: Props['styles'];
  @Input() ariaLabel!: Props['ariaLabel'];
  @Input() dataTestId!: Props['dataTestId'];
}
