<!--
  Mitosis automatically generates this file.
  Do not edit this file directly.
  The file source is in `packages/core/atoms/sidenav/SideNavItem.lite.tsx`
-->

<template>
  <GiSideNavRow :ariaHidden="ariaHidden"
    ><button
      :id="id"
      :class="
        classes({
          disabled: disabled,
          hasAction: !!actions,
          className: ['gi-side-nav-item_', className],
        })
      "
      :style="styles"
      :value="value"
      @click="async (event) => onClick && onClick(event)"
      :data-testid="dataTestId"
      :aria-current="ariaCurrent"
      :aria-label="ariaLabel"
      :aria-controls="ariaControls"
      :tabIndex="ariaHidden ? -1 : tabIndex"
    >
      <slot />
    </button>
    <template v-if="!!actions">
      <GiBox
        :className="
          actionClasses({
            expandable: true,
          })
        "
        ><slot name="actions">{{ actions }}</slot></GiBox
      >
    </template>
  </GiSideNavRow>
</template>

<script setup lang="ts">
import classes, { actionClasses } from './SideNavItem.styles';
import GiSideNavRow from './SideNavRow.vue';
import GiBox from '../Box.vue';

export type Props = {
  id?: string;
  actions?: any;
  value?: string;
  disabled?: boolean;
  className?: string;
  styles?: Record<string, string>;
  onClick?: (event: any) => void;
  ariaHidden?: boolean | 'true' | 'false';
  ariaCurrent?: 'page' | 'step' | 'location' | 'date' | 'time' | 'true' | 'false' | boolean;
  ariaLabel?: string;
  ariaControls?: string;
  tabIndex?: number;
  dataTestId?: string;
};

const props = withDefaults(defineProps<Props>(), {
  ariaHidden: false,
  id: undefined,
  disabled: false,
  actions: undefined,
  className: undefined,
  styles: undefined,
  value: undefined,
  onClick: undefined,
  dataTestId: undefined,
  ariaCurrent: undefined,
  ariaLabel: undefined,
  ariaControls: undefined,
  tabIndex: undefined,
  children: undefined,
});
</script>
