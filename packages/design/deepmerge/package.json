{
  "version": "1.0.4",
  "name": "@ogcio/ds-deepmerge",
  "description": "The Gov IE design system deepmerge utility.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "format": "prettier 'src/**/*.ts' --write",
    "format:check": "prettier 'src/**/*.ts' --check",
    "lint": "eslint 'src/**/*.ts'",
    "build": "tsup"
  },
  "files": [
    "dist"
  ],
  "prettier": "@ogcio/ds-prettier-config",
  "dependencies": {
    "deepmerge": "^4.3.1"
  },
  "devDependencies": {
    "@ogcio/ds-eslint-config": "workspace:*",
    "@ogcio/ds-prettier-config": "workspace:*",
    "eslint": "^9.23.0"
  }
}
