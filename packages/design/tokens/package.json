{
  "name": "@ogcio/design-system-tokens",
  "version": "1.22.1",
  "description": "The Government of Ireland Design System tokens.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm build:tokens && pnpm build:source",
    "build:tokens": "tsx scripts/build.ts",
    "build:source": "tsup",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@ogcio/design-system-tokens-builder": "workspace:*",
    "tsup": "^8.4.0"
  }
}
